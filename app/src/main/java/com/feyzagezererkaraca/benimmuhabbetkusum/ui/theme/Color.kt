package com.feyzagezererkaraca.benimmuhabbetkusum.ui.theme

import androidx.compose.ui.graphics.Color

object AppColors {
    // Background Colors
    val BackgroundHome = Color(0xFFEBEFEF)
    val BackgroundNutrition = Color(0xFFEBEFEF)
    val BackgroundHealth = Color(0xFFEBEFEF)
    val BackgroundAbout = Color(0xFFEBEFEF)

    // Navigation Bar
    val NavBarBackground = Color(0xFF311D6B)
    val NavBarIndicator = Color(0xFF3498DB)

    // Top App Bar
    val TopBarBackground = Color(0xFF311D6B)

    // Card Colors

    val CardColor = Color(0xFFDEE1E1)
    val CardColorSecondary = Color.White.copy(alpha = 0.9f)
    val CardGradientStart = Color.Gray
    val CardGradientEnd = Color.Magenta
    val BeneficialFoodCardBackground = Color(0xFFE8F5E9)
    val HarmfulFoodCardBackground = Color(0xFFFFEBEE)

    // Text Colors
    val TextPrimary = Color.White
    val TextSecondary = Color.Gray
    val BeneficialFoodTextPrimary = Color(0xFF2E7D32)
    val BeneficialFoodTextSecondary = Color(0xFF1B5E20).copy(alpha = 0.8f)
    val HarmfulFoodTextPrimary = Color(0xFFC62828)
    val HarmfulFoodTextSecondary = Color(0xFFB71C1C).copy(alpha = 0.8f)

    // Icon Colors
    val IconSelected = Color.White
    val IconUnselected = Color.Gray

    // Common Colors
    val White = Color.White
    val Black = Color.Black
    val Transparent = Color.Transparent

    // Button Colors
    val ButtonColorPrimary = Color(0xFF4CAF50) // Yeşil
    val ButtonColorSecondary = Color(0xFFE53935) // Kırmızı
}